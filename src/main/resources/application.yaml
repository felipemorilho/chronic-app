spring:
  application:
    name: chronicpain
  datasource:
    url: jdbc:postgresql://localhost:5432/chronic_pain_db
    username: chronicapp
    password: youtube2025
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

server:
  port: 8080
